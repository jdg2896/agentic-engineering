[project]
name = "agentic-engineering"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyyaml",
    "requests",
    "feedparser",
    "anthropic>=0.50.0",
    "jinja2",
    "ruamel.yaml",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.uv]
package = false
